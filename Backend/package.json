{
  "name": "socialloop-backend",
  "dependencies": {
    "axios": "^1.13.6",
    "bcrypt": "^6.0.0",
    "cloudinary": "^2.9.0",
    "cookie-parser": "^1.4.7",
    "cors": "^2.8.6",
    "dotenv": "^17.3.1",
    "express": "^5.2.1",
    "init": "^0.1.2",
    "jsonwebtoken": "^9.0.3",
    "mongoose": "^9.3.0",
    "multer": "^2.1.1"
  },
  "scripts": {
    "dev": "nodemon server.js",
    "start": "node server.js",
    "build": "npm run build:frontend && npm run sync:frontend",
    "build:frontend": "npm --prefix ../Frontend ci && npm --prefix ../Frontend run build",
    "sync:frontend": "node scripts/sync-frontend-build.js"
  },
  "devDependencies": {
    "nodemon": "^3.1.14"
  }
}
